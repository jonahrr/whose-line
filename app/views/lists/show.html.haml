%p#notice= notice

%p
  %b Title:
  = @list.title
%p
  %b Date:
  = @list.date

%p
  %b Speakers:
  %ul
    - @list.list_entries.each do |l|
      %li
        = l.speaker.name
        = button_to 'Remove', { action: 'destroy', controller: 'list_entries', id: l.id }, method: :delete

%p
  Add someone to the list:
  = render 'list_entries/form'

= link_to 'Edit', edit_list_path(@list)
\|
= link_to 'Back', lists_path
